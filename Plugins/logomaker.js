/*const tts = require("google-tts-api");
let mergedCommands = [
  "",
];

module.exports = {
  name: "logomaker",
  alias: [...mergedCommands],
  description: "All Logo maker Commands",
  start: async (
    Atlas,
    m,
    {
      inputCMD,
      text,
      pushName,
      prefix,
      doReact,
      args,
      mentionByTag,
      mime,
      isMedia,
      quoted,
    }
  ) => {
    switch (inputCMD) {
      case "":
        await doReact("🧩");

        break;

      default:
        break;
    }
  },
};*/
